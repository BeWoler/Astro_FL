---
import type { ILinkProps } from "./link.interface";

const { title, href } = Astro.props as ILinkProps;
---

<a
  href={href}
  class='font-medium text-gray-300 hover:text-blue-200 transition-all delay-75'
  >{title}</a
>
